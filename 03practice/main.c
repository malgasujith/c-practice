# include ACEDEC_H

int main(){
    int min,max;
    minandmax(&min,&max);
    printf("%d,%d",min,max);
    for(i=0,i<N;i++){
        scanf("%d",&arr[i]);
    }
     for(i=0,i<N;i++){
        scanf("%d",arr[i]);
    }
    printf("acending order array");
    acending();
    printf("decending order array");
    decending(int arr);
    return 0;
}
